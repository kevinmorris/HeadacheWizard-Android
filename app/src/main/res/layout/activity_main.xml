<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
                name="viewModel"
                type="pro.kevinmorris.headachewizard.viewmodel.MainViewModel" />
    </data>

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true"
            tools:context=".ui.MainActivity">

        <RelativeLayout
                android:id="@+id/main_toolbar"
                android:layout_width="match_parent"
                android:layout_height="70dp">

            <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/edit_factors_button"
                    android:layout_width="50dp"
                    android:layout_height="match_parent"
                    android:src="@drawable/icon_edit_factors"
                    android:layout_alignParentRight="true"
                    android:layout_centerVertical="true"
                    android:layout_marginRight="20dp"
                    android:scaleType="centerInside"
                    android:onClick="@{() -> viewModel.editFactors()}"/>

            <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/edit_today_button"
                    android:layout_width="50dp"
                    android:layout_height="match_parent"
                    android:src="@drawable/icon_edit_today"
                    android:layout_toLeftOf="@id/edit_factors_button"
                    android:layout_centerVertical="true"
                    android:layout_marginRight="20dp"
                    android:scaleType="centerInside"
                    android:onClick="@{() -> viewModel.editToday()}"/>

            <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/calendar_button"
                    android:layout_width="50dp"
                    android:layout_height="match_parent"
                    android:src="@drawable/icon_calendar"
                    android:layout_toLeftOf="@id/edit_today_button"
                    android:layout_centerVertical="true"
                    android:layout_marginRight="20dp"
                    android:scaleType="centerInside"
                    android:onClick="@{() -> viewModel.showCalendar()}"/>


        </RelativeLayout>


        <androidx.fragment.app.FragmentContainerView
                android:id="@+id/fragment_container"
                android:name="androidx.navigation.fragment.NavHostFragment"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_below="@id/main_toolbar"
                app:defaultNavHost="true"
                app:navGraph="@navigation/navigation">

        </androidx.fragment.app.FragmentContainerView>

    </RelativeLayout>
</layout>